# 本 Skill 的死亡判据

> 从 SKILL.md 拆分。

1. 连续 10 次使用，Next Steps 完成率（下次会话实际执行了上轮 Next Steps）< 30%
2. 最近 10 次 project_status.md 更新的"本轮完成"字段与项目实际文件变更的匹配率 < 50%（需独立审查者或人类抽查判定）
3. memory 时效性检查成为机械标注——标了"需验证"但从不去验证（判定：标注"需验证"的条目在下 2 次会话中仍未被验证）。**升级规则**：连续 3 次标注"需验证"且从未验证的条目 → 在 MEMORY.md 该行末尾标 `[STALE]`，下次会话提醒用户删除或确认保留。已标 `[STALE]` 且再经 2 次会话未处理的 → 由 AI 提议直接删除。
4. 最近 20 次符合触发条件（产生了状态变更）的会话中，本 skill 被实际调用 < 5 次
